var characters = require('./data/characters.json');

var charArr = characters.data.results
// 1. Print out every name to the terminal.
  // charArr.forEach(function (el) {
  //   console.log(el.name);
  // })

//2. Get a list of every character and their ID, as an object.
  // var charObj = {};
  // charArr.forEach(function (el) {
  //   charObj[el.name] = el.id;
  // });
  // console.log(charObj);
